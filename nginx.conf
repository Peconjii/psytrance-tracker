server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router handles URLs like /events or /reset-password in the browser, so any path
    # that isn't a real file gets index.html instead of a 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}
